export declare type Libraries = ("drawing" | "geometry" | "localContext" | "places" | "visualization")[];
export interface LoadScriptUrlOptions {
    googleMapsApiKey: string | "";
    googleMapsClientId?: string;
    version?: string;
    language?: string;
    region?: string;
    libraries?: Libraries;
    channel?: string;
    mapIds?: string[];
}
export declare function makeLoadScriptUrl({ googleMapsApiKey, googleMapsClientId, version, language, region, libraries, channel, mapIds }: LoadScriptUrlOptions): string;
